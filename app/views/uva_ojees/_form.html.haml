= form_for [@oier,@uva_ojee] do |f| 
  - if @uva_ojee.errors.any? 
    #error_explanation
      %h2 #{pluralize(@uva_ojee.errors.count, "error")} prohibited this account from being saved:
      %ul
        - @uva_ojee.errors.full_messages.each do |msg| 
          %li= msg
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :userid
    = f.text_field :userid
  .actions
    = f.submit
