= form_for [@oier,@pku_ojee] do |f| 
  - if @pku_ojee.errors.any? 
    #error_explanation
      %h2 #{pluralize(@pku_ojee.errors.count, "error")} prohibited this account from being saved:
      %ul
        - @pku_ojee.errors.full_messages.each do |msg| 
          %li= msg
  .field
    = f.label :name
    %br
    = f.text_field :name
  .actions
    = f.submit
