= form_for [@oier,@zerojudge_ojee] do |f| 
  - if @zerojudge_ojee.errors.any? 
    #error_explanation
      %h2 #{pluralize(@zerojudge_ojee.errors.count, "error")} prohibited this account from being saved:
      %ul
        - @zerojudge_ojee.errors.full_messages.each do |msg| 
          %li= msg
  .field
    = f.label :name
    %br
    = f.text_field :name
  .actions
    = f.submit
